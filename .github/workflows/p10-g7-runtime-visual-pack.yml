name: Gate 7 Runtime Visual Pack Tests

on:
  push:
    branches:
      - work/v1.54-p10-multiview-completion
    paths:
      - 'Experimental/P10_Lab/p10_lab/gate7_runtime.py'
      - 'Experimental/P10_Lab/p10_lab/gate7_visual_pack.py'
      - 'Experimental/P10_Lab/p10_lab/preview_nodes.py'
      - 'Experimental/P10_Lab/tests/test_gate7_runtime_visual_pack.py'
      - '.github/workflows/p10-g7-runtime-visual-pack.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate7-runtime-visual:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ['3.12']
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Install focused runtime dependencies
        run: python -m pip install numpy pillow

      - name: Compile Gate 7 runtime visual modules
        run: |
          python -m py_compile Experimental/P10_Lab/p10_lab/gate7_runtime.py
          python -m py_compile Experimental/P10_Lab/p10_lab/gate7_visual_pack.py
          python -m py_compile Experimental/P10_Lab/p10_lab/preview_nodes.py

      - name: Run Gate 7 runtime visual focused tests
        working-directory: Experimental/P10_Lab
        run: python -m unittest -v tests.test_gate7_runtime_visual_pack

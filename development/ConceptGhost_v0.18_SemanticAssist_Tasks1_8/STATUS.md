# Status — Semantic Assist Tasks 1–8

COMPLETE at automated code/test level:

1. Optional semantic controls + contract.
2. Isolated A-only sidecar / official node / fail-open worker contract.
3. SemanticBundle cleanup, macro classes, confidence, uncertainty, boundaries and source-UV sampling.
4. Conservative semantic ground/sky geometry priors with OFF/degraded no-op behavior.
5. Semantic-aware Hero Mesh boundary policy; native E1/E2/E3 diagnostics remain unchanged.
6. Non-destructive semantic metadata/grouping for Maya/USD/FBX handoff; no topology split by class.
7. Exactly one Semantic Assist branch wired into official Stage 9–14/Master workflows; OFF by default.
8. Semantic diagnostics + manifests + consumer report + fail-open packaging.

PENDING:

9. Promotion benchmark on several representative real scenes, including isolated GroundingDINO/SAM execution on the target Windows/GPU, real Maya/FBX validation, objective OFF-vs-ON metrics and user visual approval.

Promotion is intentionally blocked until Task 9 passes. The branch must not be treated as default-ON production semantics yet.
